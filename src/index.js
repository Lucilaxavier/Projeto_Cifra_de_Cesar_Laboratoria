const cipher = {
  encode: (codificarfrase, offset)=> {
let codificarfrase = document.getElementById('palavra a cifrar digitada').value;
let offset = Numero(document.getElementById('deslocamento')).value;
console.log(cipher);

let encode = ("");
let numero = ("");
let opcao = ""

if (opcao === "encode") {
        }
    document.getElementById("palavra a cifrar digitada").innerHTML;
}

}

//import cipher from './cipher.js';

console.log(cipher);
